[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neural-algorithmic-reasoning-combinatorial-optimization"
version = "0.1.0"
description = "Verification-first trace-supervised neural algorithmic reasoning for exact knapsack search"
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { file = "LICENSE" }
authors = [{ name = "jorsacademy" }]
keywords = [
  "neural algorithmic reasoning",
  "combinatorial optimization",
  "knapsack",
  "dynamic programming",
  "branch and bound",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "numpy>=1.26,<3",
  "safetensors>=0.4,<1",
  "torch>=2.2,<3",
]

[project.optional-dependencies]
dev = [
  "mypy>=1.11,<2",
  "pytest>=8,<10",
  "pytest-cov>=5,<8",
  "ruff>=0.6,<1",
]

[project.scripts]
narcopt = "narcopt.cli:main"

[project.urls]
Repository = "https://github.com/jorsacademy/neural-algorithmic-reasoning-combinatorial-optimization"
Issues = "https://github.com/jorsacademy/neural-algorithmic-reasoning-combinatorial-optimization/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "C4", "PIE"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = true
show_error_codes = true
packages = ["narcopt"]
mypy_path = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=src/narcopt --cov-branch --cov-report=term-missing --cov-fail-under=84"
